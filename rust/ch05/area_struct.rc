#[derive(Debug)]
struct Rectangle{
	width: u32,
	height: u32,
}

fn area(rect: &Rectangle) -> u32{
	rect.width * rect.height
}

fn main(){
	let rect = Rectangle{
		width: 30,
		height: 50,
	};

	let area1 = area(&rect);

	println!("area for rectangle {:?} is {}",rect,area1);
} 
